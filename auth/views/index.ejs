<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #667eea, #764ba2);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: white;
    }
    .card {
      border-radius: 1rem;
      box-shadow: 0 8px 20px rgba(0,0,0,0.2);
      padding: 2rem;
      text-align: center;
    }
    .btn-custom {
      background-color: #ff7eb3;
      color: white;
      margin: 5px;
    }
    .btn-custom:hover {
      background-color: #ff4f9b;
      color: white;
    }
  </style>
</head>
<body>

  <div class="card">
    <h1 class="mb-4">Welcome<% if(user){ %>, <%= user.email %><% } %>!</h1>
    
    <% if(!user){ %>
      <!-- Show login/signup if user not logged in -->
      <a href="/login" class="btn btn-custom">Login</a>
      <a href="/signup" class="btn btn-custom">Sign Up</a>
    <% } else { %>
      <!-- Show protected routes + logout if user logged in -->
      <a href="/url" class="btn btn-custom">Go to URL Shortener</a>
      <a href="/logout" class="btn btn-custom">Logout</a>
    <% } %>
    
    <a href="/" class="btn btn-custom">Home</a>
  </div>

</body>
</html>
